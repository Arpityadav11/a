<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
    rel="stylesheet" 
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" 
    crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="style.css">
</head>
<body class="mode">
        <header>
            <div class="icon-head">
                <i class="fa-regular fa-comments">
                </i> 
                <h1>WebChat</h1>
            </div>
            <div class="menu">
            <input type="text" placeholder="Search chat rooms..." id="search-bar">
            </div>
        </header>
    <main class="main">
            <%for(let chat of chats){%>
                <div class="chat-room" id="general-chat">
                    <div class="msg">
                        <p><h2>From: <i><%=chat.from%></i></h2></p>
                        <p id="msg"><%=chat.msg%></p>
                    </div>
                        <div class="btns">
                            <form method="GET" action="/chats/<%=chat.id%>/edit">
                                <button class="btn btn-outline-warning edit-btn ">edit</button>
                            </form>
                            <form method="POST" action="/chats/<%=chat.id%>?_method=DELETE">
                                <button class="btn btn-outline-danger dlt-btn ">DELETE</button>
                            </form>
                        </div>
                    <p>recieved by:<i><%=chat.to%></i></p>
                    <div class="unimp">
                    <hr>
                    <p><%=chat.created_at.toString().split(" ")[4]%></p>
                    <p><%=chat.created_at.toString().split(" ").slice(0,4).join("-")%></p>
                    </div>
                </div>
                <br><br>
            <%}%>  
    </main>
    <footer>
        <form method="get" action="/chats/new">
            <button id="create-room-btn">New chat</button>
        </form>
    </footer>
</body>
</html>

